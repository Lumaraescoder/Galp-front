import React from 'react';

import { SpinnerContainer, SpinnerElement } from './SpinerStyled';

const Spinner = () => {
  return (
    <SpinnerContainer>
      <SpinnerElement />
    </SpinnerContainer>
  );
};

export default Spinner;
